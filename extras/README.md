# AzerothCore High Elf custom race - extra files

All subdirectories here are optional files from the base mod.

The base mod must be installed first, then install the optional extras on top of the base installation.

Each directory should have at least a README.md with basic information.